// 状态层·统一入口（HTML版·模块化）
(function() {
  window.STORE = {
    Backpack: window.Backpack,
    Stats: window.Stats,
    Persistence: window.Persistence,
    GameStore: window.GameStore,
    gameStore: window.gameStore
  };
})();