// 工具层·统一入口（HTML版·模块化）
(function() {
  window.UTILS = {
    Joystick: window.Joystick,
    Camera: window.Camera,
    SpriteAnimator: window.SpriteAnimator,
    dirFromVector: window.dirFromVector,
    drawPlayer: window.drawPlayer,
    WeatherEffect: window.WeatherEffect,
    AudioManager: window.audioManager,
    Collision: window.Collision,
    logger: window.logger
  };
})();